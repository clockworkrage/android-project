<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textViewStyle">@style/MainFont</item>
        <item name="android:windowBackground">@color/colorWhite</item>
    </style>

    <style name="DarkTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorLightBlue</item>
        <item name="colorAccent">@color/colorBrightOrange</item>
        <item name="android:textViewStyle">@style/MainFont.Ligth</item>
        <item name="android:windowBackground">@color/colorGray</item>
    </style>

    <style name="MainFont" parent="@android:style/TextAppearance.Holo">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textSize">@dimen/font_size</item>
    </style>

    <style name="MainFont.Ligth">
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="MainButton" parent="@android:style/Widget.Button">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textSize">@dimen/font_size</item>
    </style>

    <style name="MainButton.Wide">
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="NavBar">
        <item name="android:background">#FFFFFF</item>
    </style>

    <style name="ToolbarColoredBackArrow" parent="AppTheme">
        <item name="android:textColorSecondary">@color/colorGray</item>
    </style>

</resources>
